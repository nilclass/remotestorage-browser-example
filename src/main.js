
require.config({
  paths: {
    "remotestorage" : "../lib/remotestorage",
    "jquery"        : "../lib/jquery-1.8.2",
    "bootstrap"     : "../lib/bootstrap"
  }
});

define(['app'], function() {});